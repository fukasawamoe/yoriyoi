<main class="w-full flex justify-center">
  <%= form_with model: @category do |f| %>
    <%= render 'shared/error_messages', object: @category %>
    <div class="form-container rounded">
      <div class="form-group p-8">

        <%= f.text_field :name, placeholder: "スケジュール名", class: 'rounded' %>

        <%= f.fields_for :category_tasks do |category_task| %>
          <%= render 'category_task_fields', f: category_task %>
        <% end %>

      </div>
    </div>
    <div class="flex justify-center p-8">
    <%= link_to_add_association 'スケジュールを追加', f, :category_tasks, class:"btn btn-accent" %>
    <div class="flex justify-center p-8">
      <button class="btn btn-accent" type="submit">スケジュールを保存する</button>
    </div>
    </div>
  <% end %>
</main>