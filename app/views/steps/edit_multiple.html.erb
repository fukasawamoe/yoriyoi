<section>
  <div class="container mx-auto">
    <div class="text-center">
      <div class="w-full mx-auto " style="background: #ffe6e8; padding: 20px; border: 2px dashed #ffb6c1; box-shadow: 0 0 0 5px #ffe6e8; -moz-box-shadow: 0 0 0 5px #ffe6e8; -webkit-box-shadow: 0 0 0 5px #ffe6e8; font-size: 100%;">
        <div class="chat chat-start flex items-end justify-start">
          <div>
            <%= image_tag "cat_information.png", class: 'w-24 rounded-full ring' %>
          </div>
          <div class="chat-bubble bg-gray-50 w-full mx-auto m-5">
            <p>キミの理想のジブンは"<%= @goal.ideal_self %>"にゃんだね〜〜！</p>          </p>
            <p>理想のためににゃにを頑張ればいいかにゃ？</p>
            <p>* たとえば、" 腹筋20回 "を週に" 3 "回がんばる！のように書くのにゃ</p>
            <p>みっつすべて書かなくてもいいにゃ</p>
            <p>まずはひとつだけでもがんばろ〜！</p>
          </div>
        </div>
        <div class="justify-center p-10">
          <%= form_with url: update_multiple_steps_path, method: :put,local: true do |f| %>
            <% @steps.each do |step| %>
              <%= f.fields_for "steps[#{step.id}]", step, include_id: false do |step_form| %>
              <%= render 'shared/error_messages', object: step_form.object, class:"mt-5" %>
                <div class="flex flex-col md:flex-row items-center justify-center">
                  <div><%= step_form.text_field :action, class:'input input-bordered max-w-xs' %> を週に</div>
                  <div class="whitespace-nowrap"><%= step_form.number_field :times_set, min: 1, max: 7,class:'input input-bordered w-20 text-center' %> 回がんばる!</div>
                </div>
              <% end %>
            <% end %>
            <div class="p-5 flex flex-col sm:flex-row justify-center items-center">
              <button class="btn btn-accent m-2" type="submit" name="home"><i class="fa-solid fa-check"></i> 登録</button> |
              <button class="btn btn-secondary m-2" type="submit" name="goal"><i class="fa-regular fa-gem"></i></i> 目標の編集</button>
            </div>
          <% end %>
        </div>
      </div>
      <div class="p-5 flex flex-col sm:flex-row justify-center items-center">
        <%= link_to raw('<i class="fa-solid fa-house"></i> ホームへ'), home_index_path, class: 'btn btn-outline btn-secondary mt-10' %>
      </div>
    </div>
  </div>
</section>